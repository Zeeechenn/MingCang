# Atlas Dormant Integration — retained boundaries

> Historical integration record archived 2026-09-17. Current configuration and
> evidence are in STATUS; work order is only ROADMAP. Do not act on old remote
> alignment, rollback commands, model plans or benchmark figures from the archive.

The historical baseline `4882d49` and dormant integration `9820143` identify past
changes, not a request to reset today's dirty tree or restore an old database.
Full original and SHA-256: [archive digest](evidence/document_archive_digest.md).

- Atlas code/structures can exist with `ATLAS_ENABLED=false`; default official
  signals, risk/size/stops, scheduler, postmarket and test2 do not thereby gain Atlas behavior.
- Dormant switches do not isolate shared schemas/dependencies/loaders/helpers;
  changes there still require parity checks and independent review.
- Trusted memory requires reviewed evidence; raw/pending/legacy candidates are
  not automatically promoted by LLM/tool output.
- Historical integration was additive/data-preserving; its one approved table
  rebuild does not authorize new migrations or production maintenance.
- Atlas/test4 investment-effect shadow and source-gated research-positioning
  shadow are distinct lanes. Neither is a demonstrated production benefit.
- Adapter/recall/long-term integration and unstarted shadow arms are conditional
  backlog, not a new work queue or claimed completion; re-propose only through ROADMAP.
- Rollback must preserve current user data and other changes. Diagnose on an
  isolated snapshot; no blind checkout/reset or old-DB restore. Push/release remains explicit.

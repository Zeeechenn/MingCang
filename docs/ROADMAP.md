# 明仓 / MingCang — 路线图（进行中与待做）

> 2026-07-03 全面重写；2026-07-15 状态刷新。方向裁决：**放弃"把所有信号融合成一个综合大分"的路线**，
> 转向**功能位分工**（选股 / 止跌避雷 / 止盈 / 浮动止盈各用各的公式）+ **模块化**
> （各模块独立演进、可单独更新或接入）。已完成/已证伪/挂起的里程碑压缩为文末
> 归档索引一行一条，详情走 `CHANGELOG.md` 与 `docs/dev/`。

## 北极星

- 目标：真实帮助 owner 实盘炒股、尽可能提高收益；回撤容忍约 20%。
- 最终产出物：**每日盘后操作面板**（买入候选 + 持仓体检 + 避雷警示 + 复盘归因），所有模块的输出向它汇聚。
- 分工哲学：确定性规则做打分与风控底线；**LLM 做裁量**（候选内选股、清仓决断、加减仓时机、复盘提炼——test2 治疗臂已验证的四个价值点），不做机械打分。
- 基准纪律（全局强制）：任何回测/回放报告必须带**同池等权持有基线**与**最大单票贡献占比**（防止把分化市运气读成能力；test2 治疗臂 alpha 主要来自单票天孚 +43%）。

## 当前接手入口（2026-07-15 刷新）

| 工作线 | 当前状态 | 第一动作 | 停止条件 |
|---|---|---|---|
| **M58 出场影子臂** | 价格系**选股位已终局证伪**（全量 695 支×966 日网格，DSR=0.0，详见 CHANGELOG/LEADER 档案）；仅剩**出场通道影子臂在跑**（4-6 周，2026-07 起） | 影子臂到期后按预注册裁决出场参数 | 单窗口/单 regime 不定稿；试验记账；生产切换需用户确认 |
| M54 新闻层 v2 | 纯新闻腿弱（ICIR 0.167）、公告增强=时段效应、**flow 融合无稳定增量（07-06 全覆盖回放）**——三变体均不判 GO；现役=**前向攒 IC 天**（pyramid 默认，flow 腿自动带上） | 每日盘后自动采集；IC 天≥20 + 跨 regime 后复判 | 裁决前不改权重不接 live；legacy 生产链零接触 |
| **M68 新闻金字塔生产镜像与风险槽位** | **连续试用已接线**：真实生产库双跑、事件风险镜像、反事实 API/界面/反馈账本，以及 test2 v2 独立 `C_pyramid_shadow` 共同窗口对比已完成；涨跌方向仍是影子证据 | 从首个真实 close-confirmed C 日开始连续累积；同时补冻结证据重放、持久化提取缓存与 token 对照门 | 不回填 C 历史；事件风险灰度须过数据/触发/可解释性门；方向须另过 M54 IC/ICIR/单调/跨 regime 门并由 owner 确认 |
| M59 盘后操作面板 | 四硬规则+条件卡+风险预算+准备度分+反方审视全接线；**裁量层灰度已开**（07-05 判断门盲裁 B9:A0 后 owner 拍板） | 观察灰度首批实盘卡（质量/截断率/额度），攒判断门二轮素材 | 不自动下单；建议带理由与风险；buy/watch 措辞红线 |
| M60 观察哨 | 触发源全家桶在役（R6 价格/lhb/flow/论点验证/证伪）；2b 第二时间入场影子台账攒样本中 | 2b 样本≥20 后裁决时机规则；触发源按周末体检漏网持续进化 | observe-only；样本不足不下结论 |
| M63 日常编排 | **运转中**：六命令面（盘前/盘中/盘后/周末/研究/喂观点），LLM 只烧盘后 | 按日常跑；周末体检漏网喂触发器进化 | 语言守卫禁买卖词；关注面=test2∪标的1∪持仓非全库 |
| M64 Live Track 安全收口 | **代码完成，未启用**：公开编排与私人台账/池/日志物理分界；子集生成 fail-closed | 以脱敏 schema 建本地 state，首日只做只读 dry-run 与人工验产物 | 不接券商、不自动下单、不写 DB；任一硬门失败不产子集 |
| **M66 仓库结构治理** | **首批落地**：稳定 core 已无静态 `backend.tools` 反向依赖；相关测试按领域归位；前端 API/live 已收进 services；旧入口保留兼容 | 继续把 workflow 内部工具调用迁到稳定门面，并分批降低根层测试与平铺页面数量 | 每批不改业务逻辑、信号/风控、DB schema、Provider 语义、scheduler/API 合同；每批独立可回滚 |
| **M67 A/HK/US 多市场完整接入** | **小池灰度已启动，晋升 HOLD**：HK 2 只、US 3 只已完成数据/研究/影子信号/回放闭环；CN 正式链不变 | 继续积累灰度样本，补 US 公告源、真实双源故障演练与正式交易日历后再复判 | 本轮回放显著落后同池等权持有且单票贡献 50.77%；保持影子、不下单、不发提醒、不扩大全市场 |
| 量化 v2（M61 §7.5 遗留） | **首轮实跑未过门**（07-06：IC 0.0215/ICIR 0.098 vs 门 0.04/0.40，样本达标） | 资金流历史攒长（滴灌+新浪兜底已在役）后可复跑，工具 `m61_quant_walkforward` | 预注册纪律：不得读作恢复 weight_quant 的证据；过门+全门槛+用户确认才谈晋升 |

## M68 新闻金字塔生产镜像与风险槽位

### 决策与边界

- 当前证据支持的不是“单靠情绪稳定预测涨跌”，而是：**极端情绪、重大事件与未来波动/风险放大有更明确的关联**。因此 M68 把新闻金字塔拆成两个功能位：
  1. **事件/波动风险线**：识别重大事件、情绪极值、传播扩散、证据冲突和潜在波动，先进入生产镜像与人工试用；
  2. **涨跌方向线**：保留 legacy/pyramid 方向分反事实，只做 shadow，不因事件风险线可用而自动获得正式权重。
- “切生产”分两层：立即让 pyramid 读取同日真实 active 数据并进入可见影子面；只有过门后才允许它替代 legacy、影响正式 `sentiment_score` 或综合结论。
- M68 不改正式 `signals`、production weights、止盈止损、仓位和 scheduler 时序；不自动下单。test2 原 A/B 状态不读不写，新增 C 臂只从首条真实 shadow row 开始派生。
- M68 已 rebase 到 M67/v0.7.0；不修改多市场 provider、HK/US 数据合同或市场写入边界。

### Phase 0 — 生产镜像数据契约

- [x] 新增独立 `news_shadow_runs` 运行记录，不复用正式 `signals`，按 `symbol + as_of + profile` 幂等保存。
- [x] 同一条记录保存 legacy 分数/摘要、pyramid news/flow/composite/confidence、反事实差值和 `would_change_action`，避免两套快照无法比较。
- [x] 保存触发原因、AttributionCard、事件时间线、证据 URL/source/content depth、source diversity、降级 flags、模型/缓存/token/耗时/错误，做到每个异常可定位到数据/触发/抽取/融合/运行层。
- [x] 每支股票显式区分 `evidence` / `verified_no_news` / `fetch_failed` / `no_evidence` / `score_failed`，抓取失败不再伪装成“无新闻”。

### Phase 1 — 真实 active 双跑与触发补全

- [x] 在 M63 盘后流程的独立 shadow step 上运行，不修改 `_postmarket_news_sentiment()` 和正式聚合器。
- [x] 手动镜像默认取 active CN；M63 盘后取“持仓 + test2 + 标的1”当日关注面，不使用静态 test3。legacy 与 pyramid 使用同一 `as_of`、DB 新闻窗口和价格快照。
- [x] 向 L1 trigger 注入真实价格涨跌与 20 日成交量比；缺失时保留 `price_volume_input_missing`，不得静默跳过。
- [ ] 将进程内 shared digest / last-signal 复用迁为持久化、带日期/模型/证据版本的缓存；重启后同输入同输出。
- [ ] 保留金字塔 token 优势：相对 v2-full 节省目标 ≥50%，预算超限必须显式降级且不拖垮盘后流程。

> 已完成 run 级跨重启幂等与按 `namespace + tier` 的进程缓存隔离；未完成的是提取结果持久化、证据 hash 冻结重放和生产样本 token 对照，不得把现有幂等误报成全部完成。

### Phase 2 — 可诊断 API 与试用界面

- [x] 提供按日期/股票查询 shadow runs、总体健康、三桶复核队列和 legacy/pyramid 分歧的只读 API。
- [x] 前端新增“新闻金字塔试用”面：正式 legacy、pyramid 影子、反事实差值、触发原因、归因卡、证据时间线和中文降级解释同屏展示。
- [x] 默认审查三类样本：全部 `would_change_action`、全部高重要性未触发、基于 run id 稳定抽样的对照组，避免只看成功案例。
- [x] 反馈类型固定为：漏抓/过时/重复/关联错误/事件分类错误/情绪方向错误/重要性错误/触发阈值错误/融合稀释/解释不可用；反馈与 run id 绑定且可选具体 evidence id，早期试用别名只作兼容。
- [ ] 修复后使用冻结证据快照重放，禁止重新抓一批新闻后把数据变化误认成修复效果。

### Phase 3 — 事件风险槽位灰度

- [x] 在不改正式方向分的前提下，已把 pyramid 输出接入 M63 盘后“事件关注”镜像区：事件等级、置信、主因、thesis recheck、触发与降级 flags。
- [ ] 高证据冲击不得因低证据平滑策略被压掉；低证据剧烈跳变必须降置信并提示复核，不得伪装成确定方向。
- [x] 初始镜像使用有界样本：M63 固定优先覆盖 test2 全 25 只与全部真实持仓，再向标的1/其他关注面分配剩余额度；有效上限不得低于 `test2 ∪ 持仓`，避免 C 臂与 A/B 不同池。手动 CLI 仍可用 `--limit 20`，并保留 `NEWS_SHADOW_ENABLED` 关闭开关。

> 事件镜像已将“L0 高重要性却未触发”直接升为高优先级错误样本，且显示低置信/降级 flags；但仍需前向试用证明所有“低证据剧烈跳变”都能被稳定捕获，因此第二项不提前勾选。

### Phase 4 — 方向线继续影子与独立晋级

- [x] 方向性指标与风险识别指标分账：新增独立 `C_pyramid_shadow`，在首个真实 C 日之后重算共同窗口 A/B/C、同池等权基线与最大单票贡献；方向线看 h1/h3/h5 IC/命中，事件线看后续绝对涨跌幅。
- [x] M63 盘后在 M68 落库后自动派生 C 臂 JSON/Markdown；无完整收盘或无真实 M68 row 时 fail-closed，不回填、不用旧日数据冒充当日。
- [ ] 方向线晋级沿用 M54：IC ≥0.04、ICIR ≥0.40、分桶单调、≥20 非重叠 IC 日、跨 regime、无 PIT/fallback/provenance blocker。
- [ ] 即使方向门通过，也只按固定样本 25%→50%→100% 灰度；每阶段保留 legacy 双跑和 owner 显式确认。

> 2026-07-16 首次真库门检：7 月 15 日 test2 为 25/25 收盘 + 25/25 信号；7 月 16 日检查时为 0/25 收盘 + 2/25 信号。因此只完成真库 fail-closed 演练，未事后回填 7 月 15 日来制造 C 臂成绩；首个真实 C 日等待下一个完整 close-confirmed 日。

### 验收与回滚门

- active 股票状态可解释覆盖 100%，运行成功率 ≥99%；价格/成交量触发输入覆盖 ≥95%。
- 高重要性公告漏触发为 0；触发窗口正文覆盖目标 ≥80%；所有 action-changing 分歧完成人工抽查。
- 进程重启后冻结输入重放无不可解释漂移；provider/模型/缓存/证据版本完整落盘。
- 事件风险灰度不以方向 IC 未过门为阻塞，但不得改变正式方向分；方向生产切换必须同时通过 M54 统计门和 owner 人工门。
- 配置保留 `legacy | dual | pyramid` 路由和 fail-closed 回滚；任何数据健康、预算、延迟或降级率越界即退回 `dual/legacy`。

## M66 仓库结构治理

目标是让目录重新表达业务职责，同时保持现有模块化单体、运行入口和发布合同不变；不借结构治理
重写业务逻辑，也不把项目拆成微服务或复杂 monorepo。

### 放置规则

1. 正式逻辑按领域归入 `data / analysis / backtest / evidence / research / memory / decision / portfolio`；
   M 编号只保留在路线图、变更史、schema/version 和旧命令兼容入口中。
2. `backend.tools` 只保留 CLI/维护/评估适配器；业务模块不得反向依赖工具实现。
3. 日常编排归工作流层；API、scheduler 和 agent 只依赖稳定门面，不直接复用工具私有函数。
4. 测试随被迁移模块一起移动，最终按领域区分 unit / contract / integration；不做一次性 195 文件搬家。
5. 前端后续按 app / features / services / ui / fixtures / styles 收敛；单独批次只改结构，不改 UX/API。
6. `docs_public` 继续作为站点源，`docs` 保持维护者资料；同名页面必须明确唯一权威来源。

### 执行阶段

- [x] **Phase 0 基线与冲突隔离**：确认当前 WIP 已收口、工作树干净、CodeGraph 可用；记录正式模块
  反向依赖 `backend.tools` 的边和 668 文件基线。
- [x] **Phase 1a 稳定 core 依赖倒置**：资金流/Tavily、lookahead 审计、IC 统计、量化基线、研究
  reference/watchtower path 与 M63 编排门面已有领域归属；架构守卫禁止 core 新增静态
  `backend.tools` 依赖，旧模块继续作为兼容别名或 CLI。
- [ ] **Phase 1b workflow 适配器收口**：`backend.workflows.m63_daily` 内仍有面向维护工具的动态调用；
  只在对应能力具备稳定领域门面时逐项迁移，不为清零而复制逻辑。
- [x] **Phase 2a 关联测试归位**：首批 8 个根层测试迁入 architecture/backtest/data/evidence/workflows，
  pytest 契约和旧入口兼容测试保留。
- [ ] **Phase 2b 根层测试分批下降**：当前仍有 190 个根层测试；后续只随所属模块变更搬迁，避免
  一次性重排造成审查噪音。
- [x] **Phase 3a 前端 services 边界**：API 与 live runtime 迁入 `frontend/src/services/`，生产代码改用
  canonical import，旧 `src/api.ts`/`src/live.ts` 保留 re-export 兼容并有测试守卫。
- [ ] **Phase 3b 前端功能聚合**：后续再拆 app shell、shared UI 与 reports/stock 等大功能；每批以
  TypeScript/Vitest/ESLint/build 和真实浏览器流程为硬门。
- [ ] **Phase 4 data/docs 收口**：在稳定门面保护下细分 market/news/fundamentals/storage/provider；
  同步清理文档双源和本地运行产物位置。该阶段涉及 Provider/DB/cache/PIT，必须单独评审。

### 每批验收

- 旧 CLI/import 入口至少保留一个版本周期；迁移表记录 canonical path 与 compatibility path。
- 不改变 signals、positions、止盈止损、production weights、scheduler 时序、API schema 或数据库。
- 聚焦测试、架构边界测试和 `make verify` 全绿；CodeGraph 无新增循环或意外反向依赖。
- 每批独立提交、可独立回滚；不混入 runtime DB、日志、模型、个人台账或生成报告。

## M67 A/HK/US 多市场完整接入

目标是在不改变 A 股现有正式信号语义的前提下，把港股和美股接入自选、行情、财务、新闻/公告、
长期研究、盘后信号、持仓风控、回测/回放、数据质量与前端日常操作面。三个市场共享接口和审计合同，
但分别使用自己的交易日历、时区、基准、费用、成交/结算、价格约束和准入门；禁止把 A 股参数复制给
HK/US。首轮只做股票研究和模拟决策闭环，不扩张到期权交易或真实券商下单。

### 执行阶段

- [x] **Phase 0 统一身份与迁移**：引入稳定 `instrument_id`/`asset_key` 和市场元数据；价格、财务、
  新闻、公告、信号与持仓的唯一性必须包含市场，旧 A 股 symbol API 保持兼容；生产库迁移可回滚。
- [x] **Phase 1 分市场规则与日历**：建立 CN/HK/US 独立 profile，覆盖币种、时区、基准指数、交易时段、
  交易日、同日卖出约束、每手/碎股能力、价格限制和回测费用；scheduler 按各市场收盘确认后运行。
- [x] **Phase 2 港美股数据链**：HK/US 日线至少两源可降级，接入并规范化财务、新闻/公告、公司行动和
  基准指数；所有记录带 provider、adjustment、fetched_at、PIT 日期和 freshness/quality 状态。
- [x] **Phase 3 研究与决策链**：技术、情绪、长期研究、组合/持仓和报告按市场 profile 运行；正式信号
  准入从单一 CN 开关改成 `CN production + HK/US symbol allowlist gray`，灰度信号必须显式标记。
- [x] **Phase 4 测试与迁移门**：聚焦 contract/integration 测试、schema authority、provider 降级、
  时区/跨日、PIT、费用、回放基线和全量 `make verify` 全绿；CN 结果须有非回归证明。
- [x] **Phase 5 小池灰度与回放**：港股和美股各选少量高流动性标的，先补足 close-confirmed 历史数据，
  再生成 observe/gray 信号并做小范围回放；报告必须含同池等权基线、最大单票贡献、成本后收益、
  覆盖缺口和退化标记。灰度只写研究/模拟记录，不产生真实订单。
- [x] **Phase 6 前端完整适配**：股票路由、缓存和列表 key 全部使用 `market + symbol` 身份；行情、新闻、
  财务、归因和 K 线按市场隔离；持仓与盈亏按 CNY/HKD/USD 分组，禁止跨币种直接求和；个股页显式展示
  CN/HK/US 各自权重、结算、交易、手数、价格保护、时区和 gray/observe 边界，并接入 PIT 财务来源。

### 晋升门

- 每个灰度市场：价格历史覆盖率 100%，两年覆盖率 100%，财务覆盖率至少 80%，新闻/公告缺口显式；
  单一 provider 故障时可降级且 provenance 不丢失。
- 回放只使用各市场已收盘交易日和当时可见数据；费用/复权/汇率口径写入工件，禁止跨市场混用。
- 正式全量晋升需灰度期间无 schema/PIT/时区/重复写入事故，且回放不劣于同池等权基线的判断必须
  排除单票集中贡献；本里程碑只完成小池灰度，不自动打开全市场。
- `PRODUCTION_SIGNAL_MARKETS={CN}` 的既有语义保持兼容；HK/US 只能通过显式 symbol allowlist 进入灰度。

### 2026-07-15 灰度裁决

- 灰度池：`HK:00700`、`HK:09988`、`US:AAPL`、`US:MSFT`、`US:NVDA`；全部仅生成
  `gray/shadow` 研究记录，仓位为 0，不触发提醒、订单或真实持仓。
- 分市场规则已拆开：CN 保持现有 `0.6 technical / 0.4 sentiment` 正式链；HK 灰度使用
  `0.65 / 0.35`，US 灰度使用 `0.75 / 0.25`；HK/US 不复用 CN 训练的 quant，均保持 `quant=0`。
- 截至 2026-07-14 的同池等权回放：策略成本后收益 `+127.97%`，同池等权持有 `+1232.93%`，
  最大单票策略贡献 `50.77%`。因此本轮全量晋升裁决为 **HOLD**，只能继续小池灰度。
- 晋升缺口：US SEC 公告接口本轮返回 403（实际公告覆盖 0/3）；实时灰度只成功使用一个价格源，
  双源降级目前只有 contract 测试证据；正式交易所日历依赖未落地时使用“工作日 + 新鲜收盘 bar”
  fail-closed 兜底；结构化情绪 LLM 本轮退化为空结果并显式回落中性。上述缺口清零前不得扩大灰度。
- 前端验收：CN 正式页、HK/US 灰度页和非白名单观察页均显示真实边界；桌面与移动 smoke 覆盖三市场
  个股路由，真实浏览器灰度回放用于核对港美股白名单、独立规则、币种与 PIT 财务展示。

## 触发待命（不占活跃执行队列）

| 工作线 | 已完成裁决 | 重新立项条件 |
|---|---|---|
| M57 记忆系统自进化 | Phase 1-3 已落地；真实 trace/miner/治理演练完成，自动 trusted=0；本轮 Memory 价值因 reference-set 偏差无法精确归因 | 冻结可核验记忆附录、重新预注册并保留 human promotion gate |
| M65 研究可信度 | Phase 0-2 完成并裁决 `HOLD_STOP_PHASE_3_4`；通用 Evidence Card/结构化 Gate 保留，Serenity 主效应总体为负 | 先补适用性门、逐 claim 证据绑定和公平评审基线，再预注册 v1.1；不得事后改判本轮 |

## 归档索引（一行一条，详情不再占活跃表）

- **M65 研究可信度收口**：2026-07-15 Phase 0-2 完成，20 例×四臂/20 次单盲评分裁决 HOLD；Both 四项质量均下降且幻觉率恶化，Phase 3-4 停止；Evidence Card 与结构化 ResearchReportGate 保留。完整结果见 `CHANGELOG.md` Unreleased。
- **M57 本轮自进化闭环**：2026-07-15 完成真实 research trace、过滤 miner 与治理台归档演练；候选保持 pending/archived，自动 trusted=0。后续仅按上方触发条件复测。
- **M64 代码交付**：安全漏斗已随 v0.6.2 完成并归档；活跃表只保留尚未执行的本地 state/dry-run 人工启用门。
- **M61 数据基建统筹改造**：2026-07-06 收官——手册库/发牌表/七簇品类/消费端接线/判断门盲裁（B2:A2，数据面 PASS）全落地；价格层清零（300759 含首窗）；fund_flows 经新浪兜底 provider 破局（84 支~1 万行）。计划全文 `docs/dev/M61_DATA_FOUNDATION_PLAN.md`。
- **M62 GitHub 大版本发布**：v0.6.0→v0.6.1 已发布（2026-07-05），公开 CI 四 job 全绿；发布门五项全过。
- **M63 复盘编排**：已交付并转日常运行（见上表活跃行）。
- **操作闭环一、二期**：论点合流/演练场/触发器/条件卡/风险预算/准备度分/反方审视/交易台账/分散模式全落地（2026-07-05）。

- **M44 / ATLAS**：2026-07-03 终局 **REJECT**——Gate-B 全量历史回填（1108 realized）判定 REJECT（delta -0.59pp）；门判据放宽变体 v2a-d 全部更差（-2.3~-2.7pp，标签层反向）；Stage 2b 组合回放 overlay 8.0% vs 基线 27.95%（吞吐塌缩）。**结论：研究工件折进短线过滤器整条线证伪。** L0-L4 记忆、案例结构、复盘闭环、证据台账**保留为研究质量与记忆基础设施**（归 M57 使用，不参与打分）。Gate-B/Stage 2b 证据积累停线，观测库保留 `~/.stock-sage/gate_b.db`。历史详见 `docs/ATLAS_MERGE.md`。
- **M56 AI 产业预警雷达**：未立项撤销（2026-07-03，方向收敛，砍）。
- **M51 外部借鉴**：冲 star 战略滞后，随之挂起；已落地部分（D1 统计门合约、report-pack v1 adapter）保留使用。详案 `docs/dev/M51_EXTERNAL_BORROWING_PLAN.md`。
- **M29**：假设注册/前向证据机制（registry、readiness、ledger 工具链）**并入 M58** 作为基础设施，不再单列工作线。
- **M21.4**：裁决已执行（v2 出场参数不改），exit_sweep 大样本通道并入 M58 Phase 1。
- **M32 / M24.3 / M25 / M12 / M10.5 / M4 / M5**：触发待命项集体归档；若未来触发条件满足再按需复活。
- 更早里程碑（M0–M55、v0.x 发布史）：见 `CHANGELOG.md`。

## 纪律备忘（从证伪中花钱买来的）

1. 好公司 ≠ 好的短线买点：长期研究工件（标签/深研）不得折进短线信号（M44 教训）。
2. 单边行情里的动量神话不可信：任何组件必须跨 regime 验证（6 月动量 ICIR 2.59 幻觉教训）。
3. 降级必须显式：组件退化必须告警 + 溯源落库（quant placeholder 静默降级一个月教训）。
4. 小样本盈利先查集中度：先看剔除最强单票后还剩多少（test1 兆易、test2 天孚教训）。
5. 权重由 OOS 定，中途不手调（M54 纪律，推广到所有功能位）。

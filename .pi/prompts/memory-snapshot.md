# Memory Snapshot

Run:

```bash
python3 -m backend.agent.cli memory-snapshot --pretty
```

Summarize project-owned memory by scope/category, layered memory status, recent
audit entries and any stale or risky memory-management issue.
